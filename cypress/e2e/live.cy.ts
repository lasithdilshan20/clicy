
describe('Live Test', () => {
  it('runs REPL steps', () => {
    // Previous commands
    

    // Current command (will be highlighted in Cypress)
    cy.visit("https://www.google.com/");
  });
});
